"""
    This program sets up the TUPLE of departments then
    prints it, then prints each department separately.
"""
dept = ('Sales', 'Production', 'Accounts')
print(dept)

# Now, let's print each department on a different line.
for item in dept:
    print(item)
